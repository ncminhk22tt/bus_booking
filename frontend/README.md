# Frontend

React Vite + Tailwind frontend scaffold for the current bus booking backend.

## Run

1. Install dependencies:
   `npm install`
2. Start dev server:
   `npm run dev`

## Environment

Create `frontend/.env` if you want a custom backend URL:

`VITE_API_BASE_URL=http://localhost:5000`
